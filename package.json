{
  "name": "anthropic-proxy",
  "version": "1.0.0",
  "description": "Anthropic-to-OpenAI API proxy built with Bun + TypeScript",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "bun-types": "^1.1.0",
    "typescript": "^5.5.0"
  }
}
