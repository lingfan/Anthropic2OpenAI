# Proxy listen port
PORT=7878

# OpenAI-compatible upstream base URL (no trailing path)
TARGET_URL=http://127.0.0.1:1234

# Optional: force model name sent upstream (leave empty to use client's model)
TARGET_MODEL=

# Set to true to log converted request/response bodies
DEBUG=false
